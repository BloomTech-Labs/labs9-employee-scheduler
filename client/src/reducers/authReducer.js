import something from '../actions'

const initialState = {
  user: null,
  token: ''
}

export const authReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}
